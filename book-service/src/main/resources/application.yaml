spring:
  application:
    name: book-service

  datasource:
    url: jdbc:postgresql://localhost:5433/books_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8082