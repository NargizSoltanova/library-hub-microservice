spring:
  application:
    name: user-service


  datasource:
    url: jdbc:postgresql://localhost:5432/users_db
    username: postgres
    password: 123456

  jpa:
    hibernate:
      ddl-auto: validate

server:
  port: 8081
